export const schoolName =
  process.env.NEXT_PUBLIC_SCHOOL_NAME?.trim() || "My School";

export const schoolLocation =
  process.env.NEXT_PUBLIC_SCHOOL_LOCATION?.trim() || "";

export const userName =
  process.env.NEXT_PUBLIC_USER_NAME?.trim() || "Teacher";
